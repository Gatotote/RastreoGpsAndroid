<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base solo para el arranque; la UI real la pinta Compose (RastreoTheme).
         Barras transparentes para el modo edge-to-edge. -->
    <style name="Theme.RastreoGps" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/fondo_oscuro</item>
    </style>
</resources>
